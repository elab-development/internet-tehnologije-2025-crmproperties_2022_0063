version: "3.9"

services:
  db:
    image: postgres:16-alpine
    container_name: crmproperties_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: crmproperties
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d crmproperties"]
      interval: 2s
      timeout: 3s
      retries: 30

  app:
    build:
      # docker-compose.yaml je u ROOT-u, a projekat je u ./crm-properties
      context: ./crm-properties
      dockerfile: Dockerfile.crm-properties.frontend
    container_name: crmproperties_app
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ./crm-properties/.env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/crmproperties?schema=public

      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: postgres
      DB_NAME: crmproperties
      PGPASSWORD: postgres

      NODE_ENV: development
      NEXT_TELEMETRY_DISABLED: "1"

      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      - "3000:3000"
    volumes:
      # mountuj ceo projekat iz ./crm-properties u container /app
      - ./crm-properties:/app
      - node_modules:/app/node_modules
      - next_cache:/app/.next
    entrypoint:
      [
        "sh",
        "-c",
        "sed -i 's/\\r$//' /app/entrypoint.sh /app/studio-entrypoint.sh 2>/dev/null || true; sh /app/entrypoint.sh"
      ]

  studio:
    build:
      context: ./crm-properties
      dockerfile: Dockerfile.crm-properties.backend
    container_name: crmproperties_studio
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ./crm-properties/.env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/crmproperties?schema=public

      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: postgres
      DB_NAME: crmproperties
      PGPASSWORD: postgres
    ports:
      - "5555:5555"
    volumes:
      - ./crm-properties:/app
      - node_modules:/app/node_modules
    entrypoint:
      [
        "sh",
        "-c",
        "sed -i 's/\\r$//' /app/entrypoint.sh /app/studio-entrypoint.sh 2>/dev/null || true; sh /app/studio-entrypoint.sh"
      ]

volumes:
  db_data:
  node_modules:
  next_cache: